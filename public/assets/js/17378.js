// This file was generated by Slater.app - Sui V2

let paths = window.location.pathname.split('/')


// BC26 -> 
{ 
   const pages = ["basecamp"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/62037.js'); 
} 
 

// 404 - Index@1.0 -> 
{ 
   const pages = ["404"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52022.js'); 
} 
 

// Hashi - Index@1.0 -> 
{ 
   const pages = ["hashi"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/57685.js'); 
} 
 

// MoonShots - Index@1.0 -> 
{ 
   const pages = ["moonshots"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/58620.js'); 
} 
 

// Payments - Index@1.0 -> 
{ 
   const pages = ["payments"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/59666.js'); 
} 
 

// Privacy - Index@1.0 -> 
{ 
   const pages = ["privacy"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/59581.js'); 
} 
 

// Developers - Index@1.0 -> 
{ 
   const pages = ["developers"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/54826.js'); 
} 
 

// About - Index@1.0 -> 
{ 
   const pages = ["about"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51945.js'); 
} 
 

// BTCfi -> 
{ 
   const pages = ["btcfi-bitcoin-defi-on-sui"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51929.js'); 
} 
 

// Bug Bounty - Index@1.0 -> 
{ 
   const pages = ["bug-bounty-program"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51951.js'); 
} 
 

// Circulation - Index@1.0 -> 
{ 
   const pages = ["token-schedule"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52017.js'); 
} 
 

// Community - Index@1.0 -> 
{ 
   const pages = ["community"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51944.js'); 
} 
 

// Core/SafeBase -> 

// Core/SafeBase has not been connected to a Webflow page.

// Get Started -> 
{ 
   const pages = ["get-started"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52024.js'); 
} 
 

// Lenis Mobile -> 
{ 
   const pages = ["network", "payments", "privacy", "hashi", "founder-starter-pack", "builder-support", "programs-funding", "faq", "launch-on-sui", "project-ideas", "build-on-sui", "ecosystem", "mysticeti", "zklogin", "move", "passkey", "nautilus"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52671.js'); 
} 
 

// Network - Index@1.0 -> 
{ 
   const pages = ["network-release-schedule"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52019.js'); 
} 
 

// Platforms - Index@1.0 -> 
{ 
   const pages = ["mysticeti", "zklogin", "move", "passkey", "nautilus"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51864.js'); 
} 
 

// Press Center - Index@1.0 -> 
{ 
   const pages = ["press-center"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52023.js'); 
} 
 

// Request - Index@1.0 -> 
{ 
   const pages = ["request-for-proposals"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52015.js'); 
} 
 

// Research - Index@1.0 -> 
{ 
   const pages = ["research"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52018.js'); 
} 
 

// Security - Index@1.0 -> 
{ 
   const pages = ["security"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51950.js'); 
} 
 

// Storage - Index@1.0 -> 
{ 
   const pages = ["deflation"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/52016.js'); 
} 
 

// USDC - Index@1.0 -> 
{ 
   const pages = ["usdc"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51942.js'); 
} 
 

// Validators - Index@1.0 -> 
{ 
   const pages = ["validators"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51948.js'); 
} 
 

// AI - Index@1.0 -> 
{ 
   const pages = ["ai"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51695.js'); 
} 
 

// Core - Index@1.0 -> 
import('/assets/js/slater/50007.js')


// DeFi - Index@1.0 -> 
{ 
   const pages = ["defi"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51686.js'); 
} 
 

// Gaming - Index@1.0 -> 
{ 
   const pages = ["gaming"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51614.js'); 
} 
 

// Institutions - Index@1.0 -> 
{ 
   const pages = ["institutions-capital-markets"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51704.js'); 
} 
 

// Lenis Fix - Index@*.js -> 
{ 
   const pages = ["press-center", "get-started", "about", "community", "usdc", "research", "deflation", "token-schedule", "security", "validators", "bug-bounty-program", "privacy-policy", "terms"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/51321.js'); 
} 
 

// Home - Index@1.1.js -> 
{ 
   const pages = [""]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/50689.js'); 
} 
 

// Intro - Index@1.0 -> 
{ 
   const pages = ["intro-to-sui"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/49207.js'); 
} 
 

// [Draft] Terms - Index@1.0 -> 

// [Draft] Terms - Index@1.0 has not been connected to a Webflow page.

// [Archive] Global - Index@1.0 -> 
{ 
   const pages = ["sui-reboundv2", "sui-rebound"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('/assets/js/slater/49132.js'); 
} 
 

// [Archive] Home - Index@1.0 -> 

// [Archive] Home - Index@1.0 has not been connected to a Webflow page.

